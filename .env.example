PORT=8080
# Optional defaults (work even without UPSTREAMS_JSON)
RPC_TARGET=
SSE_TARGET=https://docs.mcp.cloudflare.com/sse

# Fan-in map (preferred): Cloudflare + Railway SSE, add Hub later
UPSTREAMS_JSON={"cf-docs":{"sse":"https://docs.mcp.cloudflare.com/sse"},"railway":{"sse":"https://mcp-stub-server-production.up.railway.app/sse"}}
